# Dependencias SOLO de desarrollo/CI — NO se despliegan en producción.
#
# schemathesis (fuzzing del esquema OpenAPI en CI) está aquí y no en
# requirements.txt porque su rama 3.x constriñe starlette a <1.x, lo que
# bloquearía el fix de seguridad de producción (starlette>=1.3.1). Al aislarla,
# la API de producción usa starlette parcheada y el fuzzing de CI corre en su
# propio entorno. pytest llega de forma transitiva (su CVE solo afecta a tests).
-r requirements.txt
schemathesis>=3.27,<4
